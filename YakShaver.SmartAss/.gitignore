# শীর্ষস্থানীয় ফাইল এবং ডিরেক্টরিগুলো
## এই ফাইলটি গিট দ্বারা ট্র্যাক করা উচিত নয়। ##

# নির্ভরতা
**/[Bb]in/
**/[Oo]bj/

# লোকাল ব্যবহারকারী-নির্দিষ্ট ফাইল
*.DS_Store
*.suo
*.user
*.userosscache
*.sln.docstates

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Rider
*.idea
*.idea/*
!.idea/workspace.xml # Ensure workspace.xml is ignored for Rider

# Mac অটো-জেনারেটেড ফাইল
.DS_Store
.AppleDouble
.LSOverride

# থাম্বনেইল ক্যাশে ফাইল (এগুলো ব্যবহারকারী-নির্দিষ্ট ক্যাশে ফাইল)
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# ফোল্ডার ভিউ সেটিংস (এগুলো ব্যবহারকারী-নির্দিষ্ট ফাইল)
Desktop.ini

# রিসাইকেল বিন / ট্র্যাশ (এগুলো অপারেটিং সিস্টেম দ্বারা তৈরি ফাইল)
$RECYCLE.BIN/

# ভিজ্যুয়াল স্টুডিও ডায়াগনস্টিকস ফাইল
*.msp
*.pdb
*.vspscc
*.vssscc
*.psess
*.vsp
*.dtb
*.launch
*.vsix
*.suo

# NuGet প্যাকেজ
*.nupkg
*.snupkg
# nuget প্যাকেজ পুনরুদ্ধার করার জন্য আবশ্যক নয়
packages/*
# ব্যতিক্রম: packages.config গিট দ্বারা ট্র্যাক করা উচিত
!packages/repositories.config
!packages/packages.config
# যদি আপনি গিট-এ প্যাকেজ ফোল্ডার যোগ করতে চান, এই ফাইলটি সম্পাদনা করুন। 
# সাধারণত, এটি সুপারিশ করা হয় না কারণ এটি সংগ্রহস্থলকে বড় করে তোলে।

# Visual Studio ক্যাশে ফাইল
*.VC.db
*.VC.VC.opendb

# .NET Core প্রোজেক্ট নির্দিষ্ট ফাইল
project.lock.json
project.fragment.lock.json
*.xproj
*.xproj.user
*.dotsettings
*.props.user
*.pubxml.user

# বিল্ড আউটপুট
[Bb]uild/
[Rr]elease/
[Dd]ebug/
xunit.runner.json

# টেস্ট আউটপুট
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.* # বিল্ড লগ ফাইল
*.VisualState.xml
TestResult.xml
nunitresults.xml # NUnit V2 টেস্ট রেজাল্ট
TEST-*.xml # NUnit V3 টেস্ট রেজাল্ট
*.trx # MSTest 

# .NET Core প্রোজেক্ট টেম্পলেট ফাইল
*.csproj.user
*.vbproj.user

# লগ ফাইল
*.log
logs/

# এনভায়রনমেন্ট ফাইল
.env
.env.*
!.env.example

# অপারেটিং সিস্টেম ফাইল
.DS_Store
._*
.Spotlight-V100
.Trashes
Icon?
Icon?

# এনভায়রনমেন্ট-স্পেসিফিক অ্যাপ্লিকেশন সেটিংস ফাইল
appsettings.json
appsettings.*.json
!appsettings.template.json # আপনি চাইলে একটি টেমপ্লেট রাখতে পারেন

# User Secrets
secrets.json
*.UserSecretsId.txt

# বিভিন্ন IDE/টুল দ্বারা তৈরি লোকাল ফাইল
.idea/
*.iml
*.bak

# Entity Framework মাইগ্রেশন (যদি আপনি স্কিমা পরিবর্তন ট্র্যাক করতে না চান)
# Migrations/

# ASP.NET Core ওয়েবসাইট অ্যাসেট (লাইব্রেরিম্যানেজার দ্বারা পরিচালিত হলে)
/wwwroot/lib/

# Node.js
node_modules/
npm-debug.log
yarn-error.log
yarn-debug.log
pnpm-debug.log 